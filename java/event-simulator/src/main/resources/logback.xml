<?xml version="1.0" encoding="UTF-8"?>
<configuration>
    <appender name="CONSOLE" class="ch.qos.logback.core.ConsoleAppender">
        <encoder>
            <!-- ISO-8601 timestamp, level padded to 5 chars, MDC component for context -->
            <pattern>%d{yyyy-MM-dd HH:mm:ss.SSS} [%thread] %-5level %logger{36} - [%X{component}] %msg%n</pattern>
        </encoder>
    </appender>

    <!-- Simulator internals at INFO; bump to DEBUG via SIMULATOR_LOG_LEVEL env var -->
    <root level="${SIMULATOR_LOG_LEVEL:-INFO}">
        <appender-ref ref="CONSOLE" />
    </root>

    <!-- Reduce Kafka client noise; it's very chatty at INFO -->
    <logger name="org.apache.kafka" level="WARN" />
</configuration>
